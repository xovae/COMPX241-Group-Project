Basic extension for chrome browser.
To run it, open up chrome extensions and use the dev tools to load it as an extension.
Then right click on the extension to open a console which should show load times.
Opening the extension GUI and clicking the button will run another example search and 
present its loading time.